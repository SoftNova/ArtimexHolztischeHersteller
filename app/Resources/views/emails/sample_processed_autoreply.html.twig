{{  'app.sample.processed.mail.header' |
trans ({
    '%supplier_mail%':supplier_mail,
    '%sample_no%' : sample.id
},
"messages",
locale)
}}
{% for item in sample.materialSamples %}
    {{ 'app.sample.mail.repetitive.content' |
    trans ({
        '%product_name%' : item.getTranslatedName(locale),
    },
    "messages",
    locale)
    }}
{% endfor %}
{{ 'app.sample.mail.static.content' |
trans ({
    '%client_email%' : sample.clientEmail,
    '%client_company%' : sample.clientCompany,
    '%client_first_name%' : sample.clientFirstName,
    '%client_last_name%' : sample.clientLastName,
    '%country%' : sample.clientCountry,
    '%city%' : sample.clientCity,
    '%state%' : sample.clientStateOrProvidence,
    '%address_1%' : sample.clientAddress1,
    '%address_2%' : sample.clientAddress2,
    '%postal_code%': sample.clientPostalCode,
    '%client_phone%' : sample.clientPhone,
    '%comment%' : sample.clientComment
},
"messages",
locale)
}}



